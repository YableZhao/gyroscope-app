apiVersion: apps/v1
kind: Deployment
metadata:
  name: game-service
  namespace: multimodal-platform
  labels:
    app.kubernetes.io/name: game-service
    app.kubernetes.io/component: game
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: game-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: game-service
        app.kubernetes.io/component: game
    spec:
      containers:
      - name: game-service
        image: multimodal-game-service:latest
        ports:
        - containerPort: 8084
          name: http
        env:
        - name: GIN_MODE
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: GIN_MODE
        - name: PORT
          value: "8084"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: DATABASE_URL
        - name: REDIS_ADDR
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: REDIS_ADDR
        - name: WEBSOCKET_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: WEBSOCKET_SERVICE_URL
        livenessProbe:
          httpGet:
            path: /health
            port: 8084
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8084
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "250m"
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false

---
apiVersion: v1
kind: Service
metadata:
  name: game-service
  namespace: multimodal-platform
  labels:
    app.kubernetes.io/name: game-service
    app.kubernetes.io/component: game
spec:
  selector:
    app.kubernetes.io/name: game-service
  ports:
  - port: 8084
    targetPort: 8084
    name: http
  type: ClusterIP